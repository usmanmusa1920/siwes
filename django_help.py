
"""
  If any issue when using the default user model
  (from django.contrib.auth.models import User)
  use the below tricks

export DJANGO_SETTINGS_MODULE=mop.settings
import django
django.setup()

"""
